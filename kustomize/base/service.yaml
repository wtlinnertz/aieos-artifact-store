apiVersion: v1
kind: Service
metadata:
  name: aieos-artifact-store
  labels:
    app.kubernetes.io/name: aieos-artifact-store
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aieos-artifact-store
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
